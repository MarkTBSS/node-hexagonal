export const sendNotification = async (message) => {
    // ในตัวอย่างนี้ เราจะใช้ console.log เพื่อแสดงการแจ้งเตือน
    // ในการใช้งานจริง คุณอาจใช้บริการแจ้งเตือนเช่น Email, SMS, หรือ Push Notification
    console.log(`Notification: ${message}`);
};